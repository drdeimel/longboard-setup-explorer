/**
 * Shared UI text palette.
 *
 * Centralizes frequently reused muted text colors so global contrast tuning
 * can be done in one place.
 */
export const UI_TEXT_PRIMARY = '#E6F1FF'
export const UI_TEXT_SECONDARY = '#a3b3c9'
export const UI_TEXT_MUTED = '#ADC4E6'

